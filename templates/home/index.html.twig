{% extends 'base.html.twig' %}

{% block title %}Accueil - MDT LSPD{% endblock %}

{% block content %}
    <div class="container">
        <div class="row text-center text-light pt-4">
            {% if app.user %}
                <p> Bonjour, {{ app.user.rank }} {{ app.user.matricule}} {{ app.user.firstname }} {{app.user.lastname}}.</p>
{#                <p>Vous avez effectué à ce jour : {{ app.user.getStats }} arrestations 2 recensement de civil</p>#}
                <br>
                <br>
                <hr>
                <div class="row">
                    <div class="col-md-4">
                        <div class="text-center">
                            <a href="{{ path('app_civil_index') }}">
                                <img class="img-fluid" src="{{ '/assets/img/document.png' }}" style="" alt="Listes des civils">
                            </a>
                            <br>
                            <b>
                                <a href="{{ path('app_civil_index') }}" class="text-decoration-none text-light">
                                    Recensement civil
                                </a>
                            </b>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="text-center">
                            <a href="{{ path('app_arrestation_index') }}">
                                <img src="{{ '/assets/img/arrestation.png' }}" alt="liste arrestations" class="">
                            </a>
                            <br>
                            <b>
                                <a href="{{ path('app_arrestation_index') }}" class="text-decoration-none text-light">
                                    Arrestations
                                </a>
                            </b>
                        </div>
                    </div>
                    <div class="col-md-4">
                        <div class="text-center">
                            <a href="{{ path('app_judiciary_case_index') }}">
                                <img src="{{ '/assets/img/casier-judiciaire.png' }}" alt="Listes casiers judiciaire">
                                <br>
                            </a>
                            <b>
                                <a href="{{ path('app_judiciary_case_index') }}" class="text-decoration-none text-light">Casiers judiciaire</a>
                            </b>
                        </div>
                    </div>
                </div>
            {% else %}
                <p>
                    Bonjour/Bonsoir, si vous êtes là c'est que soit vous êtes perdu, soit vous êtes sur le Serveur de <b>META RP FA</b>.
                    <br>
                    <br>

                    1 - Vous êtes perdu, je vous invite à rejoindre ce serveur GTARP : <a href="https://discord.gg/metafa">META RP FA</a> .
                    <br><br>
                    2 - Vous êtes Agent LSPD, je vous invite soit à demander la création d'un compte en contactant un Haut Gradé du LSPD
                    ou à vous <a href="{{path('app_login')}}">connecter</a> .
                </p>
            {% endif %}
        </div>
    </div>
{% endblock %}
