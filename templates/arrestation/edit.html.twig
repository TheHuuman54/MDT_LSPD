{% extends 'base.html.twig' %}

{% block title %}Modifier l'arrestation - {{ arrestation.suspect.firstname }} {{ arrestation.suspect.lastname }}{% endblock %}

{% block content %}
    {% if app.user %}
    <h1 class="text-center mt-5">Modifier l'arrestation</h1>
<div class="container">
    {{ include('arrestation/_form.html.twig', {'button_label': 'Valider'}) }}

    <a class="btn btn-outline-warning float-end" href="{{ path('app_arrestation_index') }}">Retour</a>

    {{ include('arrestation/_delete_form.html.twig') }}
</div>

    {% else %}
        <h1 class="text-center mt-5">
            ERROR 404 - NOT FOUND
        </h1>
        <p class="mt-5 text-center">La page demandée n'existe pas.</p>
    {% endif %}
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('assets/js/pictureDelete.js') }}" defer></script>
{% endblock %}
